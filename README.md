**Xen Manager**

This is a very basic plugin currently used mainly to hook start up and
tear down of domU VM's with array Start/Stop events.

*Note: this plugin is active only when the Xen kernel is booted with unRAID
as Domain-0.*
